# Production Environment Variables for DOOH React Frontend
# Copy this file to .env.production and fill in your actual values

# Application Environment
NODE_ENV=production

# API Configuration (if you have a backend)
# REACT_APP_API_URL=https://your-backend-api.azurewebsites.net
# REACT_APP_WS_URL=wss://your-backend-api.azurewebsites.net

# Azure Application Insights (for monitoring)
# REACT_APP_APPINSIGHTS_INSTRUMENTATIONKEY=your-app-insights-key

# Feature Flags
# REACT_APP_ENABLE_ANALYTICS=true
# REACT_APP_ENABLE_CAMERA=true

# Build Configuration
GENERATE_SOURCEMAP=false
INLINE_RUNTIME_CHUNK=false

# Security Headers (handled by nginx, but can be overridden)
# REACT_APP_CSP_POLICY=default-src 'self'
